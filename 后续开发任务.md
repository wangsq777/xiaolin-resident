# 小林驻留中：关怀型桌宠后续开发任务

## 1. 产品定位

“小林驻留中”不再以音乐播放器为核心，而是一只长期驻留在桌面、能够提供轻量提醒和安静陪伴的关怀型桌宠。

本地 BGM 保留为可选氛围功能：用户可以完全关闭音乐，仅使用角色陪伴和关怀提醒。桌宠不依赖网络、音乐平台账号或开发者会员。

产品体验关键词：

- 安静陪伴
- 提醒有分寸
- 用户可控制
- 本地优先
- 不制造负罪感
- 不擅自推断用户状态

## 2. 设计原则

### 2.1 关怀而非监督

- 用户没有完成提醒时，不批评、不连续催促。
- 每类提醒都可以关闭、延后或今日跳过。
- 不使用连续签到、断签惩罚或焦虑型文案。
- 提醒文案应简短、温和，避免医疗诊断或命令式表达。

### 2.2 用户掌握状态

- 工作、休闲和免打扰状态优先由用户手动选择。
- 专注计时开始后可以自动进入工作状态。
- 系统空闲时间和本地时间只能用于辅助判断。
- 不使用摄像头、麦克风或屏幕内容分析用户正在做什么。

### 2.3 本地与隐私

- 设置、提醒记录和状态全部保存在本机。
- 不要求账号登录。
- 不上传本地音频、提醒记录或活动状态。
- 不加入真人声音克隆或冒充本人说话的功能。

## 3. 角色状态模型

状态优先级从高到低：

```text
免打扰
  ↓
临时关怀提醒
  ↓
工作／专注
  ↓
休闲
  ↓
夜间睡眠
```

| 状态标识 | 触发条件 | 目标形象 | 交互表现 |
|---|---|---|---|
| `do_not_disturb` | 用户手动开启或处于安静时段 | 低动作或安静坐着 | 不弹普通提醒，只保留必要状态 |
| `reminder_drink` | 到达喝水提醒时间 | 拿水杯 | 展开喝水提醒气泡 |
| `reminder_stretch` | 连续使用电脑达到设定时间 | 起身或伸懒腰 | 提醒活动身体 |
| `reminder_eyes` | 屏幕使用达到设定时间 | 摘眼镜或望向远处 | 提醒短暂离开屏幕 |
| `working` | 用户选择工作或开启专注计时 | 拿吉他演奏 | 动作克制，减少主动打扰 |
| `leisure` | 用户选择休闲或结束专注 | 看书 | 翻页、眨眼或安静坐着 |
| `sleeping` | 进入夜间时段或长时间无操作 | 困倦／睡觉 | 降低动画和提醒频率 |
| `happy` | 用户完成一次关怀提醒 | 微笑或点赞 | 短暂回应后恢复原状态 |

当前已有并已整理的素材：

- `working`：拿吉他唱歌形象，可直接复用。
- 默认回退形象：现有站立像素形象。
- `leisure`：看书形象。
- `reminder_drink`：拿水杯形象。
- `reminder_stretch`：伸懒腰形象。
- `reminder_eyes`：揉眼睛形象。
- `sleeping`：困倦或睡觉形象。
- `happy`：完成提醒后的回应形象。
- `do_not_disturb`：戴耳机工作形象。

用户提供的原图保存在 `src/renderer/assets/character-states/source/`，处理后的透明 PNG 保存在上一级目录。`manifest.json` 记录尺寸、可见范围和来源；`Scripts/prepare-character-assets.py` 可从原图重复生成处理版本。

任何状态素材缺失时必须回退到默认形象，不能导致窗口空白或应用报错。

## 4. 关怀提醒交互

喝水提醒示例：

> 忙归忙，先喝一点水。

提醒气泡提供三个动作：

1. 喝过了
2. 10 分钟后提醒
3. 今天不再提醒

交互要求：

- 提醒出现时，小窗在原位置临时展开。
- 处理提醒后自动收起并恢复之前的角色状态。
- “稍后提醒”的时间应允许在设置中修改。
- “今天不再提醒”只影响当天和当前提醒类型。
- 免打扰期间不显示普通提醒；结束后不补发已经过期的提醒。
- 每个提醒事件只允许存在一个可见气泡，避免重复堆叠。

## 5. 主页面调整

当前 BGM 管理页后续改造为“桌宠关怀中心”，包含：

- 当前角色状态与手动状态切换
- 喝水提醒设置
- 久坐活动提醒设置
- 眼睛休息提醒设置
- 安静时段设置
- 专注计时设置
- 角色形象与素材状态
- 今日提醒完成记录
- 可选 BGM 设置
- 开机启动、置顶和退出设置

桌宠小窗要求：

- 平时只显示角色和简短状态文字。
- 提醒发生时才展开操作气泡。
- BGM 未启用时完全隐藏上一首、播放/暂停和下一首。
- BGM 启用后才显示音乐信息与三个控制按钮。
- 右键菜单始终提供“打开关怀中心”和“退出”。

## 6. 素材目录规范

当前已建立以下目录：

```text
src/renderer/assets/character-states/
├── default.png
├── working.png
├── leisure.png
├── drinking.png
├── stretching.png
├── eyes-rest.png
├── sleeping.png
├── happy.png
├── do-not-disturb.png
├── manifest.json
└── source/                  用户提供的原图副本
```

素材要求：

- PNG 格式，透明背景。
- 同一角色的画布比例和角色位置尽量保持一致。
- 静态状态至少提供 1 张图。
- 需要眨眼、翻书或弹吉他时，建议每个动作提供 2～6 帧。
- 加载失败或尺寸异常时回退到 `default.png`。

## 7. 分阶段开发任务

### 当前基线：v0.4

v0.4 基线（双窗口、BGM 扫描、角色状态、关怀中心）已全部完成。

### P0：关怀型桌宠最小可用版本

P0 最小可用版本（喝水提醒、安静时段、状态切换、角色回退）已全部完成。

### P1：日常关怀扩展

- [x] 夜间睡眠状态：进入夜间时段或长时间无操作时自动进入 sleeping 形象，降低动画和提醒频率

#### P1 已完成的验收标准

- 久坐活动提醒、眼睛休息提醒、提醒文案本地配置文件、提醒频率与延后时长自定义已实现
- 完成提醒后的 `happy` 短暂状态已实现
- 今日提醒完成次数已实现，仅保存在本机，新的一天自动重置
- 不同提醒类型不会同时堆叠显示
- 用户可以单独关闭任意提醒类型
- 桌宠位置、大小和透明度记忆，多显示器环境下能恢复到有效屏幕位置
- 鼠标穿透模式已实现
- 系统托盘／菜单栏入口已实现

### P2：专注与表现力

- [ ] 番茄钟与自定义专注时长
- [ ] 专注开始自动进入 `working`
- [ ] 专注结束自动恢复 `leisure`
- [ ] 角色逐帧眨眼、翻书和弹吉他动画
- [ ] 本地时间问候
- [ ] BGM 淡入淡出、随机播放和单曲循环
- [ ] 开机启动设置
- [ ] 打包 macOS `.app` / `.dmg`
- [ ] 打包 Windows `.exe`

## 8. 建议的数据结构

后续设置文件可采用以下结构，实际字段以实现为准：

```json
{
  "currentState": "leisure",
  "quietHours": {
    "enabled": true,
    "start": "23:00",
    "end": "08:00"
  },
  "reminders": {
    "drink": {
      "enabled": true,
      "intervalMinutes": 60,
      "snoozeMinutes": 10
    },
    "stretch": {
      "enabled": false,
      "intervalMinutes": 90
    },
    "eyes": {
      "enabled": false,
      "intervalMinutes": 45
    }
  },
  "bgm": {
    "enabled": false
  }
}
```

## 9. 测试要求

- 提醒计时器使用可注入时间，避免测试真实等待。
- 测试跨午夜安静时段，例如 `23:00—08:00`。
- 测试“今日跳过”在第二天自动恢复。
- 测试多个提醒同时到期时的队列顺序。
- 测试角色素材缺失、损坏和加载失败的回退。
- 测试 BGM 关闭时播放器控件完全隐藏。
- 测试应用重启后的设置恢复。
- 保留 Electron 双窗口 smoke test。

## 10. 暂不实现

- 医疗建议、健康诊断或强制健康目标
- 摄像头、麦克风和屏幕内容监控
- 真人声音克隆
- 冒充林家谦本人发表台词
- 云端账号系统与跨设备同步
- 复杂任务管理、社交或排行榜

这些能力与当前“安静、本地、有分寸的关怀型桌宠”定位不一致，除非未来重新确认产品方向，否则不进入开发范围。
